services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: grip
      POSTGRES_PASSWORD: grip_dev
      POSTGRES_DB: grip
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
